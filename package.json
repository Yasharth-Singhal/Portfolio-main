{
  "name": "portfolio",
  "private": true,
  "scripts": {
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "build": "npm run build --prefix client && npm run build --prefix server",
    "build:staging": "npm run build --prefix client",
    "build:prod": "npm run build --prefix client && npm run build --prefix server",
    "lint": "npm run lint --prefix client && npm run lint --prefix server",
    "format": "npm run format --prefix client && npm run format --prefix server",
    "check": "npm run build && npm run lint && npx prettier --check \"client/src/**/*.{ts,tsx,css}\" \"server/src/**/*.ts\"",
    "prepare": "husky"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "husky": "^9.0.0",
    "lint-staged": "^15.0.0",
    "prettier": "^3.8.0"
  },
  "lint-staged": {
    "client/**/*.{ts,tsx,css}": "prettier --write",
    "server/**/*.ts": "prettier --write"
  }
}
